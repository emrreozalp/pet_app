import 'package:flutter/material.dart';

const EdgeInsets bodyHorizontalPadding = EdgeInsets.symmetric(horizontal: 20.0);
const EdgeInsets bodyVerticalPadding = EdgeInsets.symmetric(vertical: 30.0);

const EdgeInsets rightPadding = EdgeInsets.only(right: 8.0);
const EdgeInsets leftPadding = EdgeInsets.only(left: 8.0);
const EdgeInsets verticalPadding = EdgeInsets.symmetric(vertical: 10.0);
const EdgeInsets stackPadding = EdgeInsets.only(top: 210.0);
const EdgeInsets cardTextsPadding = EdgeInsets.only(top: 5.0);
const EdgeInsets iconsLeftPadding = EdgeInsets.only(left: 50.0);
